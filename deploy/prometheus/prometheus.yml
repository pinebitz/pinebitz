global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "pinebitz-api"
    metrics_path: /metrics/prometheus
    authorization:
      type: Bearer
      credentials: "change-me"
    static_configs:
      - targets: ["api:8000"]
